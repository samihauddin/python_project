# Scripting

# There are seven modules that we can consider "core" in python

import sys
import os
import subprocess
import math
import random
import datetime
import json

# sys

#print(sys.version) # used for checking version

# os used to interact with operating system

#print(os.getcwd())

# subprocess manages processes

#subprocess.run(["python", "name of file"])


# math for complex math problems

pi = math.pi
pi_string = str(pi)
print(f"the value of pi is{pi_string}")

# random

#gives a random number

# datetime

whatisthedate = datetime.datetime.now()
print(whatisthedate)

#json

# stores data, good at transporting data